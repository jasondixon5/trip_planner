<p id="notice"><%= notice %></p>

<h2><%= @trip.name %></h2>

<p><%= @trip.body %></p>

<%= link_to 'Edit Trip', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>

<h3>Activities</h3>

<%= render @trip.activities %>